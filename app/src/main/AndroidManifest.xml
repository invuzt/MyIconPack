<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/sym_def_app_icon"
        android:allowBackup="true">

        <meta-data
            android:name="org.adw.ActivityPicker.THEMES"
            android:resource="@xml/appfilter" />
        <meta-data
            android:name="com.novalauncher.THEME"
            android:resource="@xml/appfilter" />
        <meta-data
            android:name="org.adw.launcher.THEMES"
            android:resource="@xml/appfilter" />
        <meta-data
            android:name="com.actionlauncher.playstore.THEME"
            android:resource="@xml/appfilter" />
        <meta-data
            android:name="app.lawnchair.icons.THEMED_ICON"
            android:resource="@xml/appfilter" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
